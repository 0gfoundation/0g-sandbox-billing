# Build outputs
/billing
/broker
/deploy
/upgrade
/verify
/setup
/user

# Contract build artifacts (reproducible via make build-contracts)
contracts/out/
contracts/cache/

# forge-std is managed separately; include only via submodule or install
contracts/lib/

# Environment secrets — all .env* files may contain real credentials
.env
.env.*

# Private keys and certificates
*.key
*.pem
*.p12
mock-payment

# Daytona
DAYTONA_API_URL=http://localhost:3000
DAYTONA_ADMIN_KEY=          # Daytona ADMIN_API_KEY value

# Redis
REDIS_ADDR=redis:6379
REDIS_PASSWORD=

# TEE / Chain
# Production: TEE_SIGNING_KEY is injected by the tapp-daemon via gRPC; leave blank here.
# For local development, use MOCK_TEE=true + MOCK_APP_PRIVATE_KEY instead.
TEE_SIGNING_KEY=            # leave blank in dev (use MOCK_TEE below)
PROVIDER_ADDRESS=0x...      # derived from TEE key if left blank
SETTLEMENT_CONTRACT=0x...   # BeaconProxy address (output of `go run ./cmd/deploy/`)
CHAIN_ID=16602
RPC_URL=https://evmrpc-testnet.0g.ai

# Billing parameters (all amounts in neuron; 1 0G = 10^18 neuron)
COMPUTE_PRICE_PER_SEC=16667  # neuron/sec billed per open sandbox (≈ 1M neuron/min)
CREATE_FEE=5000000           # neuron flat fee charged on sandbox creation
VOUCHER_INTERVAL_SEC=3600    # how often to flush compute vouchers to chain (seconds)

# Server
PORT=8080

# Local development only — do NOT set in production
# MOCK_TEE=true
# MOCK_APP_PRIVATE_KEY=0x<hex-private-key>
